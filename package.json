{
  "private": true,
  "scripts": {
    "build": "pnpm --filter ux-doctor build",
    "test": "pnpm --filter ux-doctor test",
    "typecheck": "pnpm --filter ux-doctor typecheck",
    "lint": "pnpm --filter ux-doctor lint",
    "format": "pnpm --filter ux-doctor format"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "packageManager": "pnpm@10.6.2",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "rolldown"
    ]
  }
}
